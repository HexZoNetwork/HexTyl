[Definition]
# Tight match for brute/flood at panel auth endpoint.
failregex = ^<HOST> -.*"(POST|GET) /auth/login(?:\?.*)? HTTP/.*" (401|403|404|429|444)\b
            ^<HOST> -.*"(POST|GET) /auth/login/checkpoint(?:\?.*)? HTTP/.*" (401|403|404|429|444)\b
ignoreregex =
